spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: pay-config-server
      profile: pay-asset

eureka:
  instance:
    hostname: pay-asset
  client:
    service-url:
      defaultZone: http://pay-eureka-server:8761/eureka

